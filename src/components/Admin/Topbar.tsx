'use client';

export default function Topbar() {
  return (
    <header className="topbar">
      <h1 className="topbar-title">Admin Dashboard</h1>
      <div className="topbar-controls">
        {/* future: logout, avatar, etc. */}
      </div>
    </header>
  );
}
